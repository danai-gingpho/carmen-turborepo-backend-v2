meta {
  name: 09 - UseGuards
  type: http
  seq: 9
}

put {
  url: {{host}}/api-system/cluster/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
