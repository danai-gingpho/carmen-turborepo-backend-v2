meta {
  name: 12 - ApiOperation
  type: http
  seq: 12
}

put {
  url: {{host}}/api/config/cronjobs/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
