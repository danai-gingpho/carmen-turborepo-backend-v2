meta {
  name: 11 - ApiOperation
  type: http
  seq: 11
}

post {
  url: {{host}}/api/config/cronjobs
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
