meta {
  name: 20 - UseGuards
  type: http
  seq: 20
}

put {
  url: {{host}}/api-system/business-unit/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
