meta {
  name: 12 - UseGuards
  type: http
  seq: 12
}

patch {
  url: {{host}}/api-system/business-unit/{{id}}/configs
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
