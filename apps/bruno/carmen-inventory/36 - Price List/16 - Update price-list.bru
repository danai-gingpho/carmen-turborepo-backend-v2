meta {
  name: 16 - UseGuards
  type: http
  seq: 16
}

patch {
  url: {{host}}/api/config/{{bu_code}}/price-list/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
