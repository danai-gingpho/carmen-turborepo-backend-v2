meta {
  name: 12 - Create Change Password
  type: http
  seq: 12
}

post {
  url: {{host}}/api/auth/change-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {

  }
}
