meta {
  name: 14 - UseGuards
  type: http
  seq: 14
}

delete {
  url: {{host}}/api/config/{{bu_code}}/department-user/{{id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
