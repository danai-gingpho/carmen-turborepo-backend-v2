meta {
  name: 08 - UseGuards
  type: http
  seq: 8
}

get {
  url: {{host}}/api/my-pending/store-requisition/{{tenant_id}}/{{id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
