meta {
  name: 14 - UseGuards
  type: http
  seq: 14
}

delete {
  url: {{host}}/api/my-pending/store-requisition/{{tenant_id}}/{{id}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
