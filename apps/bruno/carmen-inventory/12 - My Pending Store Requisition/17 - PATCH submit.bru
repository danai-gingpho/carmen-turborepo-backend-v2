meta {
  name: 17 - UseGuards
  type: http
  seq: 17
}

patch {
  url: {{host}}/api/my-pending/store-requisition/{{tenant_id}}/{{id}}/submit
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
