meta {
  name: 05 - Create {{tenant_id}}
  type: http
  seq: 5
}

post {
  url: {{host}}/api/my-pending/store-requisition/{{tenant_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {

  }
}
