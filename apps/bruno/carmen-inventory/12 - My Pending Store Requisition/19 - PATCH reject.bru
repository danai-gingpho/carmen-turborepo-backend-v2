meta {
  name: 19 - UseGuards
  type: http
  seq: 19
}

patch {
  url: {{host}}/api/my-pending/store-requisition/{{tenant_id}}/{{id}}/reject
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
