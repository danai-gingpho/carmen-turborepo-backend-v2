meta {
  name: 20 - UseGuards
  type: http
  seq: 20
}

patch {
  url: {{host}}/api/my-pending/store-requisition/{{tenant_id}}/{{id}}/review
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
