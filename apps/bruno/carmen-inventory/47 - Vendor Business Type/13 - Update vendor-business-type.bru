meta {
  name: 13 - UseGuards
  type: http
  seq: 13
}

patch {
  url: {{host}}/api/config/{{bu_code}}/vendor-business-type/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
