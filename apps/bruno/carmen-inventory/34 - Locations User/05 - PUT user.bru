meta {
  name: 05 - UseGuards
  type: http
  seq: 5
}

put {
  url: {{host}}/api/config/{{bu_code}}/locations/user/{{userId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
