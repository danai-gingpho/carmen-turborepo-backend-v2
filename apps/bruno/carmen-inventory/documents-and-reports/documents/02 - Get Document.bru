meta {
  name: Get document document-management
  type: http
  seq: 2
}

get {
  url: {{host}}/api/{{bu_code}}/documents/:filetoken
  body: none
  auth: bearer
}

params:path {
  filetoken: {{filetoken}}
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

vars:pre-request {
  filetoken: T02%2F410a8abd-6dec-453d-923d-dc3e11f8f39a
}

docs {
  ## Get Document by Token
  ดึงข้อมูลเอกสารรายการเดียวตาม file token
  
  Downloads a specific procurement document by its unique file token, used to view attached invoices, contracts, or delivery receipts linked to transaction records. Returns binary stream for file download.
  
  ### Path Parameters
  | Name | Type | Description |
  |------|------|-------------|
  | `bu_code` | string | Business unit code |
  | `filetoken` | string | Unique file token returned from upload |
  
  ### Headers
  | Name | Required | Description |
  |------|----------|-------------|
  | `Authorization` | Yes | `Bearer {{access_token}}` |
  | `x-app-id` | Yes | `{{x_app_id}}` |
  
  ### Permissions
  > Permission key not found in seed; review needed.
  
  ### Sample Response
  ```json
  {
    "data": "<binary stream>",
    "paginate": null,
    "status": 200,
    "success": true,
    "message": "Success",
    "timestamp": "2026-04-29T08:30:00.000Z"
  }
  ```
  
  ### Error Responses
  | Status | When |
  |--------|------|
  | 401 | Missing or invalid Bearer token |
  | 404 | File token not found in storage |
}
