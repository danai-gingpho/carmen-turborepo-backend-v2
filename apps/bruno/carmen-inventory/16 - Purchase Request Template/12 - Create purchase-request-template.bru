meta {
  name: 12 - UseGuards
  type: http
  seq: 12
}

post {
  url: {{host}}/api/{{bu_code}}/purchase-request-template
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
