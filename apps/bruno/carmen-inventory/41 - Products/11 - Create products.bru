meta {
  name: 11 - UseGuards
  type: http
  seq: 11
}

post {
  url: {{host}}/api/config/{{bu_code}}/products
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
