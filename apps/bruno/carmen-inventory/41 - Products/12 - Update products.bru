meta {
  name: 12 - UseGuards
  type: http
  seq: 12
}

patch {
  url: {{host}}/api/config/{{bu_code}}/products/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
