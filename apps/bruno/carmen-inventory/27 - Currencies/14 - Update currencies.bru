meta {
  name: 14 - UseGuards
  type: http
  seq: 14
}

put {
  url: {{host}}/api/config/{{bu_code}}/currencies/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
