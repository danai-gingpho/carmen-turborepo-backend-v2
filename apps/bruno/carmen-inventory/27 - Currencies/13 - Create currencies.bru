meta {
  name: 13 - UseGuards
  type: http
  seq: 13
}

post {
  url: {{host}}/api/config/{{bu_code}}/currencies
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
