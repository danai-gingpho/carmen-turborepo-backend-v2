meta {
  name: 09 - UseGuards
  type: http
  seq: 9
}

patch {
  url: {{host}}/api/config/{{bu_code}}/currencies/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
