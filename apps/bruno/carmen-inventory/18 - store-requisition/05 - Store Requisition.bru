meta {
  name: 05 - Create Store Requisition
  type: http
  seq: 5
}

post {
  url: {{host}}/api/{{bu_code}}/store-requisition
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {

  }
}
