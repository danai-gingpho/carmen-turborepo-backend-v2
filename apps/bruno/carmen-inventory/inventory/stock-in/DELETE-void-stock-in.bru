meta {
  name: Void stock in stock-in
  type: http
  seq: 6
}

delete {
  url: {{host}}/api/{{bu_code}}/stock-in/:id/void
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version:
}

docs {
  ## Void a Stock In
  ยกเลิกรายการรับสินค้าเข้าคลัง โดยสร้างรายการปรับลดออก ตรวจสอบว่ามีสินค้าคงเหลือเพียงพอก่อนยกเลิก

  Voids a completed stock-in record by creating a reverse adjustment-out transaction. Checks that sufficient on-hand quantity exists before voiding.

  ### Path Parameters
  | Name | Type | Description |
  |------|------|-------------|
  | `bu_code` | string | Business unit code |
  | `id` | string (UUID) | Stock In ID |

  ### Query Parameters
  | Name | Type | Required | Default | Description |
  |------|------|----------|---------|-------------|
  | `version` | string | No | latest | API version |

  ### Headers
  | Name | Required | Description |
  |------|----------|-------------|
  | `Authorization` | Yes | `Bearer {{access_token}}` |
  | `x-app-id` | Yes | `{{x_app_id}}` |

  ### Permissions
  - **Permission key**: `inventory_management.stock_in:delete`
  - **Roles**: Purchase

  ### Sample Response
  ```json
  {
    "data": {
      "id": "019638a6-2a00-7c4f-8e46-9b7a52c80c4d",
      "si_no": "SI-2026-0001"
    },
    "paginate": null,
    "status": 200,
    "success": true,
    "message": "Success",
    "timestamp": "2026-04-29T08:30:00.000Z"
  }
  ```

  ### Error Responses
  | Status | When |
  |--------|------|
  | 400 | Cannot void — insufficient on-hand quantity |
  | 401 | Missing or invalid bearer token |
  | 403 | User lacks `inventory_management.stock_in:delete` permission |
  | 404 | Stock In not found |

  > Service-layer errors propagate via TCP; see micro-business for full list.
}
