meta {
  name: 08 - UseGuards
  type: http
  seq: 8
}

post {
  url: {{host}}/api/config/{{bu_code}}/running-code
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
