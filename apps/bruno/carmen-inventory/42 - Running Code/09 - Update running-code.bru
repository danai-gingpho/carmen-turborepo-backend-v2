meta {
  name: 09 - UseGuards
  type: http
  seq: 9
}

put {
  url: {{host}}/api/config/{{bu_code}}/running-code/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
