meta {
  name: 13 - UseGuards
  type: http
  seq: 13
}

put {
  url: {{host}}/api/{{bu_code}}/vendor-product/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
