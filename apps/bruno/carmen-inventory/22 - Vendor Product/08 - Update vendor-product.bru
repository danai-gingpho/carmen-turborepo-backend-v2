meta {
  name: 08 - UseGuards
  type: http
  seq: 8
}

put {
  url: {{host}}/api/{{bu_code}}/vendor-product/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
