meta {
  name: Remove config_sql_query
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/config/{{bu_code}}/sql-query/:id
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
