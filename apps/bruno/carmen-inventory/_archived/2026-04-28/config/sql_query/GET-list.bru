meta {
  name: List config_sql_query
  type: http
  seq: 1
}

get {
  url: {{host}}/api/config/{{bu_code}}/sql-query
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
