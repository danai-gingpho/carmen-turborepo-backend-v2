meta {
  name: Create config_sql_query
  type: http
  seq: 3
}

post {
  url: {{host}}/api/config/{{bu_code}}/sql-query
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {}
}
