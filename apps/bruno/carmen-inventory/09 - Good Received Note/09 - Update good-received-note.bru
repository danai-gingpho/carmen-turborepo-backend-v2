meta {
  name: 09 - UseGuards
  type: http
  seq: 9
}

patch {
  url: {{host}}/api/{{bu_code}}/good-received-note/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
