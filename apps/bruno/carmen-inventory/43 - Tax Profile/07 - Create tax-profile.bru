meta {
  name: 07 - UseGuards
  type: http
  seq: 7
}

post {
  url: {{host}}/api/config/{{bu_code}}/tax-profile
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
