meta {
  name: 13 - UseGuards
  type: http
  seq: 13
}

patch {
  url: {{host}}/api/{{bu_code}}/credit-note/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
