meta {
  name: 01 - List {{productid}}
  type: http
  seq: 1
}

get {
  url: {{host}}/api/config/{{bu_code}}/product/location/{{productId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
