meta {
  name: 10 - UseGuards
  type: http
  seq: 10
}

patch {
  url: {{host}}/api/my-pending/purchase-request/{{tenant_id}}/{{id}}/approve
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
