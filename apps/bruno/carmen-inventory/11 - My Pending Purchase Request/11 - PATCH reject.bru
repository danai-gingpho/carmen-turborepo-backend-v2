meta {
  name: 11 - UseGuards
  type: http
  seq: 11
}

patch {
  url: {{host}}/api/my-pending/purchase-request/{{tenant_id}}/{{id}}/reject
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
