meta {
  name: 08 - UseGuards
  type: http
  seq: 8
}

put {
  url: {{host}}/api/config/{{bu_code}}/products/category/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
