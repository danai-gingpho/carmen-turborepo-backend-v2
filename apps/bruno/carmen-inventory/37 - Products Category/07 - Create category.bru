meta {
  name: 07 - UseGuards
  type: http
  seq: 7
}

post {
  url: {{host}}/api/config/{{bu_code}}/products/category
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
