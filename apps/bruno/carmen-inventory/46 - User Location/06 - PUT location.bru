meta {
  name: 06 - UseGuards
  type: http
  seq: 6
}

put {
  url: {{host}}/api/config/{{bu_code}}/user/location/{{locationId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
