meta {
  name: 04 - UseGuards
  type: http
  seq: 4
}

put {
  url: {{host}}/api/config/{{bu_code}}/user/location/{{locationId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
