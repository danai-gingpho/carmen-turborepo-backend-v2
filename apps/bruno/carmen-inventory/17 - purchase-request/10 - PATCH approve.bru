meta {
  name: 10 - UseGuards
  type: http
  seq: 10
}

patch {
  url: {{host}}/api/{{bu_code}}/purchase-request/{{id}}/approve
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
