meta {
  name: 09 - UseGuards
  type: http
  seq: 9
}

patch {
  url: {{host}}/api/{{bu_code}}/purchase-request/{{id}}/submit
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
