meta {
  name: 05 - UseGuards
  type: http
  seq: 5
}

put {
  url: {{host}}/api-system/user/cluster/{{id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    
  }
}
