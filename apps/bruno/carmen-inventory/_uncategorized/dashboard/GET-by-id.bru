meta {
  name: By id dashboard
  type: http
  seq: 2
}

get {
  url: {{host}}/api/{{bu_code}}/dashboard/:id
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
