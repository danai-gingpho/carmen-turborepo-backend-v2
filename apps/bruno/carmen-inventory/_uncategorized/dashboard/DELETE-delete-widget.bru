meta {
  name: Delete widget dashboard
  type: http
  seq: 9
}

delete {
  url: {{host}}/api/{{bu_code}}/dashboard/:id/widget/:widget_id
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
