meta {
  name: Update dashboard
  type: http
  seq: 4
}

put {
  url: {{host}}/api/{{bu_code}}/dashboard/:id
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {}
}
