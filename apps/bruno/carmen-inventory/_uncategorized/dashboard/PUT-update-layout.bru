meta {
  name: Update layout dashboard
  type: http
  seq: 6
}

put {
  url: {{host}}/api/{{bu_code}}/dashboard/:id/layout
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {}
}
