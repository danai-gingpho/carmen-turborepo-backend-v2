meta {
  name: Update widget dashboard
  type: http
  seq: 8
}

put {
  url: {{host}}/api/{{bu_code}}/dashboard/:id/widget/:widget_id
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {}
}
