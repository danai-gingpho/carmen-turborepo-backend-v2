meta {
  name: Remove dashboard
  type: http
  seq: 5
}

delete {
  url: {{host}}/api/{{bu_code}}/dashboard/:id
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}
