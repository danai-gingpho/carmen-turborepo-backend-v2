meta {
  name: Create dashboard
  type: http
  seq: 3
}

post {
  url: {{host}}/api/{{bu_code}}/dashboard
  body: json
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
  Content-Type: application/json
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {}
}
