meta {
  name: Get last receiving cost
  type: http
  seq: 2
}

get {
  url: {{host}}/api/{{bu_code}}/cost/product/:product_id/last-receiving
  body: none
  auth: bearer
}

headers {
  x-app-id: {{x_app_id}}
}

auth:bearer {
  token: {{access_token}}
}

query {
  ~version: 
}
