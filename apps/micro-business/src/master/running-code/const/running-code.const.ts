export const RUNNING_CODE_PRESET = {
  PR: {
    config: {
      A: 'PR',
      B: `date('yyyyMM')`,
      C: `running(5, '0')`,
      format: '{A}{B}{C}',
    },
  },
  PO: {
    config: {
      A: 'PO',
      B: `date('yyyyMM')`,
      C: `running(5, '0')`,
      format: '{A}{B}{C}',
    },
  },
  GRN: {
    config: {
      A: 'GRN',
      B: `date('yyyyMM')`,
      C: `running(5, '0')`,
      format: '{A}{B}{C}',
    },
  },
  CN: {
    config: {
      A: 'CN',
      B: `date('yyyyMM')`,
      C: `running(5, '0')`,
      format: '{A}{B}{C}',
    },
  },
  PL: {
    config: {
      A: 'PL',
      B: `date('yyyyMM')`,
      C: `running(5, '0')`,
      format: '{A}{B}{C}',
    },
  },
  SI: {
    config: {
      A: 'SI',
      B: `date('yyyyMM')`,
      C: `running(5, '0')`,
      format: '{A}{B}{C}',
    },
  },
  SO: {
    config: {
      A: 'SO',
      B: `date('yyyyMM')`,
      C: `running(5, '0')`,
      format: '{A}{B}{C}',
    },
  },
  SR: {
    config: {
      A: 'SR',
      B: `date('yyyyMM')`,
      C: `running(5, '0')`,
      format: '{A}{B}{C}',
    },
  },
  SC: {
    config: {
      A: 'SC',
      B: `date('yyyyMM')`,
      C: `running(5, '0')`,
      format: '{A}{B}{C}',
    },
  },
  TR: {
    config: {
      A: 'TR',
      B: `date('yyyyMM')`,
      C: `running(5, '0')`,
      format: '{A}{B}{C}',
    },
  },
};
