apiVersion: apps/v1
kind: Deployment
metadata:
  name: micro-notification
  namespace: carmen-backend
  labels:
    app: micro-notification
spec:
  replicas: 1
  selector:
    matchLabels:
      app: micro-notification
  template:
    metadata:
      labels:
        app: micro-notification
    spec:
      containers:
        - name: micro-notification
          image: carmen-micro-notification:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: tcp
              containerPort: 5006
            - name: http
              containerPort: 6006
          env:
            - name: NOTIFICATION_SERVICE_HOST
              value: "0.0.0.0"
          envFrom:
            - configMapRef:
                name: service-discovery
            - secretRef:
                name: carmen-backend-secret
            - secretRef:
                name: micro-notification-env
                optional: true
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 6006
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 6006
            initialDelaySeconds: 20
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: micro-notification
  namespace: carmen-backend
  labels:
    app: micro-notification
spec:
  type: ClusterIP
  selector:
    app: micro-notification
  ports:
    - name: tcp
      port: 5006
      targetPort: 5006
    - name: http
      port: 6006
      targetPort: 6006
